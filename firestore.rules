rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Pwoteksyon sou done itilizatè
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // Tout lòt done
    match /{document=**} {
      allow read, write: if false;
    }
  }
}